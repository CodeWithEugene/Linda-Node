{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Linda Integration Activation v1",
  "type": "object",
  "required": ["mode", "disclaimer", "activation"],
  "properties": {
    "mode": {"const": "exercise"},
    "disclaimer": {"type": "string"},
    "activation": {"type": "object", "required": ["id", "state", "digest", "evidence_ids"]}
  }
}
